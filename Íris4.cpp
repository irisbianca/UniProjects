#include <stdio.h>

int main () {
	float area, lado, per;
	
	printf ("Informe a medida do quadrado: ");
	 
	 scanf ("%f", &lado);
	 
	  per = 4 * lado;
	
printf ("Perímetro: %.2f", per);
	
	  area = lado * lado;
	  
printf ("\nÁrea: %.2f", lado);

return (0);	
}
